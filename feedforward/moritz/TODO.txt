TODO

1) BLOCKBASED
- choose for final training (max) epochs as maximum or mean+1std or median over bestepochs (check for all three folds the accuracy dynamics)
=> idea: - we do not have very fast overfitting (a few epochs too long seem not too hurt too much) but a few epochs to little do!
=>       - furthermore the final model uses more data which potentially might take more epochs to catch (but each epoch is larger so dunno)

- evaluate also first instant label and/or historylength 129 runs

- start hyperparam on merope

- check on eltanin run if error was fixed (test set scene instance numbering), i.e., validation acc can be calculated
[BEFORE FIXING VIA mode='test' in heiner's val_accuracy WE HAD THIS ERROR]
          File "training.py", line 418, in <module>
            initial_epoch=init_epoch)
          File "/mnt/antares_raid/home/augustin/binaural_audition.gitcopy/feedforward/moritz/generator_extension.py", line 338, in fit_and_predict_generator_with_sceneinst_metrics
            callbacks.on_epoch_end(epoch, epoch_logs)
          File "/mnt/antares_raid/home/spiess/anaconda3/envs/twoears_conda/lib/python3.6/site-packages/keras/callbacks.py",
        line 77, in on_epoch_end
            callback.on_epoch_end(epoch, logs)
          File "/mnt/antares_raid/home/augustin/binaural_audition.gitcopy/feedforward/moritz/training_callback.py", line 127, in on_epoch_end
            metrics_validation = calculate_metrics(self.model.scene_instance_id_metrics_dict_eval)
          File "/mnt/antares_raid/home/augustin/binaural_audition.gitcopy/feedforward/moritz/myutils.py", line 35, in calculate_metrics
            ret=('final', 'per_class', 'per_scene', 'per_class_scene'))
          File "/mnt/antares_raid/home/augustin/binaural_audition.gitcopy/recurrent/models/heiner/accuracy_utils.py", line 41, in val_accuracy
            calculate_class_accuracies_per_scene_number(scene_instance_id_metrics_dict, mode, metric=metric)
          File "/mnt/antares_raid/home/augustin/binaural_audition.gitcopy/recurrent/models/heiner/accuracy_utils.py", line 135, in calculate_class_accuracies_per_scene_number
            scene_number_class_accuracies_metrics[scene_number] += metrics
        IndexError: index 80 is out of bounds for axis 0 with size 80

- evaluationsplotting (ggf. zusammen mit changbin) machen

- finales modell auswählen, ggf. nachtrainieren

- finales modell trainieren und evaluieren

2) IN PARALLEL: INSTANT LABELS
- instant labels starten (gleich alle drei level)

- optionally finally fine tuning featuremaps, droppoutrate, learningrate

- optional: check dependence on output threshold

- finales instant modell trainieren und evaluieren

6) OPTIONALLY MORE VARIANTS
- blockbased und instant auch mit historysize 500 und 50 laufen lassen

7) FINALLY:
- put final result figs and h5 files (that can be used to replot) -- also incl final models  to github
